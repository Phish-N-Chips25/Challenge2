# requirements.txt
# Dependencies for ML Dataset Encoding & EPSS Score Prediction with GPU Support
# Compatible with Conda environments on Windows/Linux/macOS

# ============================================================================
# CORE PYTHON LIBRARIES
# ============================================================================
python>=3.8,<3.13

# ============================================================================
# DATA PROCESSING & ML CORE
# ============================================================================
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
joblib>=1.3.0

# ============================================================================
# GRADIENT BOOSTING LIBRARIES (GPU-ENABLED)
# ============================================================================
# For GPU support on Windows with CUDA:
#   conda install -c conda-forge lightgbm-gpu
#   conda install -c conda-forge py-xgboost-gpu
#
# For CPU-only (fallback):
lightgbm>=4.0.0
xgboost>=2.0.0

# ============================================================================
# AUTOML & HYPERPARAMETER OPTIMIZATION
# ============================================================================
flaml>=2.1.0
optuna>=3.0.0  # Additional optimizer (used by FLAML)

# ============================================================================
# GPU SUPPORT & ACCELERATION (OPTIONAL BUT RECOMMENDED)
# ============================================================================
# PyTorch for GPU detection and optional deep learning extensions
# Install with CUDA support:
#   conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
#
# For CPU-only:
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# CUDA Toolkit (for GPU training) - Install via conda:
#   conda install -c nvidia cuda-toolkit=12.1

# ============================================================================
# DATA VISUALIZATION
# ============================================================================
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0  # Interactive plots

# ============================================================================
# UTILITIES & PERFORMANCE
# ============================================================================
tqdm>=4.65.0  # Progress bars
psutil>=5.9.0  # System resource monitoring

# ============================================================================
# JUPYTER SUPPORT (OPTIONAL - for notebooks)
# ============================================================================
# jupyter>=1.0.0
# ipykernel>=6.22.0
# ipywidgets>=8.0.0

# ============================================================================
# CONDA ENVIRONMENT SETUP INSTRUCTIONS
# ============================================================================
# Create environment:
#   conda create -n epss_ml python=3.11
#   conda activate epss_ml
#
# Install dependencies:
#   conda install -c conda-forge --file requirements.txt
#
# For GPU support (NVIDIA RTX 5070 Ti):
#   conda install -c conda-forge lightgbm-gpu
#   conda install -c conda-forge py-xgboost-gpu
#   conda install pytorch torchvision torchaudio pytorch-cuda=12.1 -c pytorch -c nvidia
#
# Verify GPU:
#   python -c "import torch; print(torch.cuda.is_available())"
#   python -c "import xgboost; print(xgboost.__version__)"
#   nvidia-smi
